// src/pages/RegisterPage.js
import React from 'react';
import Auth from '../components/Auth'; // Ajuste o caminho

const RegisterPage = () => {
    return (
        <div>
            <Auth type="register" />
        </div>
    );
};

export default RegisterPage;
